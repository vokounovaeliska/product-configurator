# Prettier Config

Shared Prettier configuration for the monorepo.

## Installation

```bash
pnpm add -D @workspace/prettier-config
```

## Usage

Create a `prettier.config.js` file:

```js
import config from "@workspace/prettier-config";

export default config;
```
